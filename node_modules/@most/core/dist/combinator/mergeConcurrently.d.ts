/** @license MIT License (c) copyright 2010 original author or authors */
import { Stream } from '@most/types';
export declare const mergeConcurrently: <A>(concurrency: number, stream: Stream<Stream<A>>) => Stream<A>;
export declare const mergeMapConcurrently: <A, B>(f: (a: A) => Stream<B>, concurrency: number, stream: Stream<A>) => Stream<B>;
