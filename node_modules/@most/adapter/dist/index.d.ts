import { Disposable, Scheduler, Sink, Stream } from '@most/types';
export declare type Adapter<A, B> = [(event: A) => void, Stream<B>];
export declare const createAdapter: <A>() => [(event: A) => void, Stream<A>];
export declare class FanoutPortStream<A> {
    private readonly sinks;
    constructor(sinks: {
        sink: Sink<A>;
        scheduler: Scheduler;
    }[]);
    run(sink: Sink<A>, scheduler: Scheduler): Disposable;
}
export declare class RemovePortDisposable<A> implements Disposable {
    private readonly sink;
    private readonly sinks;
    constructor(sink: {
        sink: Sink<A>;
        scheduler: Scheduler;
    }, sinks: {
        sink: Sink<A>;
        scheduler: Scheduler;
    }[]);
    dispose(): void;
}
